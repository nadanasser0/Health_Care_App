import 'package:flutter/material.dart';

class SearchScreenD extends StatelessWidget {
  const SearchScreenD({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Search")));
  }
}
