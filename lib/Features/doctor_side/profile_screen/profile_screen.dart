import 'package:flutter/material.dart';

class ProfileScreenD extends StatelessWidget {
  const ProfileScreenD({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("profile")));
  }
}
